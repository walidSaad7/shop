import 'package:flutter/material.dart';

class ProudectScreen extends StatelessWidget {
static const String routeName='productScreen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Shop App'),
      ),

    );
  }
}
